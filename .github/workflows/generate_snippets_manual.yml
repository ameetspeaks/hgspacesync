name: Generate Snippets - Manual Trigger

on:
  workflow_dispatch:
    inputs:
      blog_ids:
        description: 'Comma-separated blog IDs (leave empty for pending blogs)'
        required: false
        default: ''
      limit:
        description: 'Number of blogs to process (if blog_ids not provided)'
        required: false
        default: '10'

jobs:
  generate-snippets:
    runs-on: ubuntu-latest
    
    steps:
      - name: Queue blogs for processing
        id: queue
        env:
          ADMIN_SECRET: ${{ secrets.ADMIN_SECRET }}
          API_URL: ${{ secrets.API_URL || 'https://astrologyapp.in' }}
          BLOG_IDS: ${{ github.event.inputs.blog_ids }}
          LIMIT: ${{ github.event.inputs.limit || '10' }}
        run: |
          if [ -n "$BLOG_IDS" ]; then
            echo "📋 Queueing specific blog IDs: $BLOG_IDS"
            BLOG_IDS_JSON=$(echo "[$BLOG_IDS]" | sed 's/,/,/g')
            response=$(curl -s -X POST "$API_URL/api/admin/generate-snippets" \
              -H "Content-Type: application/json" \
              -H "x-admin-key: $ADMIN_SECRET" \
              -d "{\"blog_ids\": $BLOG_IDS_JSON}")
          else
            echo "📋 Queueing pending blogs (limit: $LIMIT)"
            response=$(curl -s -X POST "$API_URL/api/admin/generate-snippets" \
              -H "Content-Type: application/json" \
              -H "x-admin-key: $ADMIN_SECRET" \
              -d "{\"limit\": $LIMIT}")
          fi
          echo "$response" | jq '.'
          echo "queued_response<<EOF" >> $GITHUB_OUTPUT
          echo "$response" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Process snippets via backend
        id: process
        env:
          ADMIN_SECRET: ${{ secrets.ADMIN_SECRET }}
          BACKEND_URL: ${{ secrets.BACKEND_URL || 'https://ameetspeaks-astrologyapp.hf.space' }}
          LIMIT: ${{ github.event.inputs.limit || '10' }}
        run: |
          echo "🚀 Starting snippet generation on backend..."
          response=$(curl -s -X POST "$BACKEND_URL/api/snippets/process-batch?batch_size=$LIMIT" \
            -H "Content-Type: application/json" \
            -H "x-admin-key: $ADMIN_SECRET")
          echo "$response" | jq '.'
          echo "process_response<<EOF" >> $GITHUB_OUTPUT
          echo "$response" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          blog_ids=$(echo "$response" | jq -r '.ids[]?' 2>/dev/null | tr '\n' ',' | sed 's/,$//')
          if [ -n "$blog_ids" ] && [ "$blog_ids" != "null" ]; then
            echo "blog_ids=$blog_ids" >> $GITHUB_OUTPUT
            echo "📝 Tracking batch with IDs: $blog_ids"
          else
            echo "⚠️ No blog IDs returned from batch"
          fi

      - name: Wait and check batch status
        if: steps.process.outputs.blog_ids != '' && steps.process.outputs.blog_ids != 'null'
        env:
          ADMIN_SECRET: ${{ secrets.ADMIN_SECRET }}
          BACKEND_URL: ${{ secrets.BACKEND_URL || 'https://ameetspeaks-astrologyapp.hf.space' }}
          BLOG_IDS: ${{ steps.process.outputs.blog_ids }}
        run: |
          echo "⏳ Waiting for batch processing..."
          # Convert comma-separated IDs to JSON array
          ids_array=$(echo "$BLOG_IDS" | tr ',' '\n' | jq -R . | jq -s .)
          
          max_attempts=20
          attempt=0
          
          while [ $attempt -lt $max_attempts ]; do
            attempt=$((attempt + 1))
            echo "📊 Check attempt $attempt/$max_attempts..."
            
            status=$(curl -s -X POST "$BACKEND_URL/api/snippets/check-batch" \
              -H "Content-Type: application/json" \
              -H "x-admin-key: $ADMIN_SECRET" \
              -d "{\"ids\": $ids_array}")
            
            echo "$status" | jq '.'
            
            is_done=$(echo "$status" | jq -r '.is_done')
            if [ "$is_done" = "true" ]; then
              echo "✅ Batch processing completed!"
              break
            fi
            
            if [ $attempt -lt $max_attempts ]; then
              echo "⏸️ Waiting 30 seconds before next check..."
              sleep 30
            fi
          done
          
          if [ "$is_done" != "true" ]; then
            echo "⚠️ Batch still processing after $max_attempts attempts."
          fi

      - name: Show results
        env:
          ADMIN_SECRET: ${{ secrets.ADMIN_SECRET }}
          API_URL: ${{ secrets.API_URL || 'https://astrologyapp.in' }}
        continue-on-error: true
        run: |
          echo "Generation completed. Fetching statistics..."
          response=$(curl -s -w "\n%{http_code}" -X GET "$API_URL/api/admin/snippet-stats" \
            -H "x-admin-key: $ADMIN_SECRET")
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          if [ "$http_code" -eq 200 ]; then
            echo "✅ Statistics:"
            echo "$body" | jq '.stats'
          else
            echo "⚠️ Warning: Could not fetch statistics (HTTP $http_code)"
            echo "Note: The API route may not be deployed yet or needs rebuild"
            echo "Snippet generation was queued successfully"
          fi

      - name: Show results
        env:
          ADMIN_SECRET: ${{ secrets.ADMIN_SECRET }}
          API_URL: ${{ secrets.API_URL || 'https://astrologyapp.in' }}
        continue-on-error: true
        run: |
          echo "Generation completed. Fetching statistics..."
          response=$(curl -s -w "\n%{http_code}" -X GET "$API_URL/api/admin/snippet-stats" \
            -H "x-admin-key: $ADMIN_SECRET")
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          if [ "$http_code" -eq 200 ]; then
            echo "✅ Statistics:"
            echo "$body" | jq '.stats'
          else
            echo "⚠️ Warning: Could not fetch statistics (HTTP $http_code)"
            echo "Note: The API route may not be deployed yet or needs rebuild"
            echo "Snippet generation was queued successfully"
          fi

